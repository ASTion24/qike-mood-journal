# 栖刻

一个私密、轻量的情绪日记与自我关怀 Web 原型。

在线访问：**https://astion24.github.io/qike-mood-journal/**

## 功能

- **记录**：先选情绪，再调整强度；可直接保存，也可展开文字与触发线索，草稿自动保存在本机
- **日记**：独立的日历与阅读页，支持跨月、同日多条记录、全文搜索、情绪筛选、编辑和删除
- **回顾**：7 / 14 / 30 天按实际日期展示感受与强度，文字中的触发线索需由用户确认后才进入统计
- **练习**：依据最近记录推荐呼吸、感官着陆、步行或环境和音；练习支持暂停、完成反馈和历史回顾
- **数据自主**：示例与个人记录分开；JSON 导出、合并恢复、单条/全部删除、旧版数据迁移
- **可访问性**：手机适配、键盘操作、弹窗焦点管理、减少动态效果偏好、即时支持入口

## 产品取舍

不要求登录，只选情绪也能保存。示例让初次访问者了解产品，但不混入个人统计。

首页围绕情绪选择展开，文字输入按需打开，保存后显示结果与下一步操作。日历和历史记录位于独立页面。界面使用浅灰白背景、墨绿文字与少量情绪色，桌面横向导航、手机底部导航；动态效果用于页面切换、书写展开、选中反馈和正在进行的练习。

分析使用设备内的关键词和统计规则，未调用大模型或提供医疗诊断。文字里出现一个词不代表它是原因，因此待确认线索不会自动计入统计。图表保留每次记录，不把不同情绪平均成一个好坏分数。

数据仅保存在当前浏览器的 `localStorage` 中，无账号同步或服务器备份。更换设备前应手动导出；清理浏览器数据会删除记录。此原型适合个人使用，不是紧急援助服务。

所有脚本、图标和 Lora 数字字体随站点托管，中文使用设备可用的无衬线字体；音频由 Web Audio 生成，无第三方运行时请求。

## 本地运行

```bash
python3 -m http.server 8000
```

然后访问 `http://127.0.0.1:8000`。

## 回归验证

安装 Python Playwright 与 Chromium 后，启动独立服务并运行：

```bash
python3 -m http.server 43127 --bind 127.0.0.1
# 在另一个终端中
python3 tests/ui_smoke.py
python3 tests/ui_calendar.py
python3 tests/ui_checkin.py
```

测试覆盖数据隔离、草稿恢复、线索确认、历史编辑、备份去重与无效文件、旧数据迁移、存储失败、练习计时/反馈，以及 320–1440px 的五种屏宽。截图写入被 Git 忽略的 `artifacts/`。

日历测试额外覆盖跨时区日期归属、同日多条记录、跨月导航、保存后回到当前月份，以及示例隔离。

记录流程测试覆盖键盘选情绪、选中时位置稳定、只记情绪、折叠与跨页草稿恢复、保存后继续记录，以及日历和全文搜索之间的切换。

## 发布

GitHub Pages 使用 `main` 分支的根目录自动部署。保持仓库名 `qike-mood-journal` 和域名不变，后续提交会更新同一链接。

Lucide 图标许可证见 `vendor/LICENSE`；Lora 字体许可证见 `vendor/fonts/Lora-OFL.txt`。
